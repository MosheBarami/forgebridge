{
  "name": "@forgebridge/core",
  "version": "0.1.0",
  "description": "The ForgeBridge engine: ports, project policy, capability router, circuit breaker, and the run pipeline. No vendor imports.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo 'lint: core (configured in M04 follow-up)'"
  },
  "dependencies": { "@forgebridge/protocol": "*" },
  "devDependencies": { "typescript": "^5.7.2", "vitest": "^2.1.8" }
}
